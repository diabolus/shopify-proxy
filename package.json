{
  "name": "shopify-cart-proxy",
  "version": "1.0.0",
  "description": "A Cloudflare worker to proxy Shopify cart mutations.",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --node-path=./node_modules"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.4",
    "@hono/zod-openapi": "^0.9.5",
    "@hono/swagger-ui": "^0.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0",
    "esbuild": "^0.20.0"
  }
}
